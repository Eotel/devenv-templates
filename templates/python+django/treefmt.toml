[global]
excludes = [
  ".devenv/**",
  ".direnv/**",
  ".venv/**",
  "venv/**",
  "**/migrations/**",
  "**/__pycache__/**",
  "staticfiles/**",
  "media/**",
]

[formatter.nix]
command = "nixfmt"
includes = ["*.nix"]

[formatter.ruff-format]
command = "ruff"
options = ["format"]
includes = ["*.py"]
